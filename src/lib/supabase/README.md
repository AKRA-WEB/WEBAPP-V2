# `src/lib/supabase`

Future Supabase client/server helpers.

Rules:

- Browser helpers may only use public/publishable keys.
- Service-role or secret-key access must stay server-side only.
- Auth/session helpers should follow current Supabase SSR guidance.
